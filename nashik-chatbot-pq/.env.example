# ===========================================
# Environment Configuration
# ===========================================
# IMPORTANT: Copy this file to .env and fill in your actual values
# Usage:
#   1. cp .env.example .env
#   2. Edit .env with your actual credentials
#   3. NEVER commit .env to git (it contains secrets!)
#
# This .env file works for both:
#   - Docker Deployment: docker-compose.yml reads this file
#   - Local Development: app/config/config.py reads this file
#
# FOR LOCAL DEVELOPMENT: Change these values:
#   POSTGRES_HOST=localhost (instead of postgres)
#   NEO4J_URL=neo4j://127.0.0.1:7687 (instead of neo4j://neo4j:7687)
# ===========================================

# ===========================================
# AZURE OPENAI CONFIGURATION
# ===========================================
# Get these from your Azure Portal > Azure OpenAI Service
# REQUIRED: You must fill these in for the app to work
AZURE_API_KEY=your_azure_api_key_here
AZURE_CHAT_ENDPOINT=https://your-resource.openai.azure.com/openai/deployments/your-chat-deployment
AZURE_EMBEDDING_ENDPOINT=https://your-resource.openai.azure.com/openai/deployments/your-embedding-deployment
AZURE_GPT5_ENDPOINT=

# ===========================================
# DATABASE PASSWORDS & CREDENTIALS
# ===========================================
# SECURITY: Change these default passwords in production!
# These are used by both Docker containers and application code

# PostgreSQL Credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_postgres_password
POSTGRES_DB=chatbot
POSTGRES_HOST=postgres  # For Docker | Use 'localhost' for local dev
POSTGRES_PORT=5432

# Neo4j Credentials
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=changeme_neo4j_password
NEO4J_URL=neo4j://neo4j:7687  # For Docker | Use 'neo4j://127.0.0.1:7687' for local dev
NEO4J_DATABASE=neo4j

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
SERVER_RELOAD=true
SERVER_LOG_LEVEL=debug

# CORS Configuration (comma-separated origins)
# Add your load balancer URL and VM IP here
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5000,http://127.0.0.1:5000,http://localhost,https://your-load-balancer-url

# Session Configuration
SESSION_SECRET=changeme_session_secret_key

# ===========================================
# OPENSEARCH CONFIGURATION (for document embeddings)
# ===========================================
# OpenSearch is used to store document chunks and embeddings
# For Docker: use 'opensearch' as host | For local dev: use 'localhost'
OPENSEARCH_HOST=opensearch  # For Docker | Use 'localhost' for local dev
OPENSEARCH_PORT=9200
OPENSEARCH_USERNAME=  # Optional: leave empty if no auth
OPENSEARCH_PASSWORD=  # Optional: leave empty if no auth
OPENSEARCH_USE_SSL=false
OPENSEARCH_INDEX_NAME=documents

# ===========================================
# DOCUMENT PROCESSING CONFIGURATION (Dataloader)
# ===========================================
# Paths for document processing and Docling models
DOCLING_BASE_DIR=/data/documents
DOCLING_ARTIFACTS_PATH=/data/docling_models/docling_all_models
DOCLING_INPUT_ROOT=/data/documents/input
DOCLING_OUTPUT_ROOT=/data/documents/output
DOCLING_VLM_MODEL=SmolDocling-256M-preview
DOCLING_LAYOUT_MODEL=docling-layout-heron

# Agent Configuration
MAX_CHAT_HISTORY=10
QUERY_TIMEOUT_SECONDS=30
